<UserControl x:Class="ArmyArranger.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArmyArranger.Views"
             xmlns:viewmodels="clr-namespace:ArmyArranger.ViewModels"
             xmlns:views="clr-namespace:ArmyArranger.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid Background="RoyalBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1" Grid.RowSpan="1" Background="AliceBlue">
            <Button x:Name="SendEmailButton" Content="Contact us" Command="{Binding ContactUs, Mode=TwoWay}"/>
            <Button x:Name="AddUnitsButton" Content="Add Units" Command="{Binding AddUnits, Mode=TwoWay}"/>
                <Button x:Name="CreateArmyListButton" Content="Create Army List" Command="{Binding CreateArmyList, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
